# Проект не має сторонніх залежностей.
# Використовується лише стандартна бібліотека Python (>= 3.11,
# потрібна для сумісності з синтаксисом list[str] / dict[str, int]
# та підтримки except* / ExceptionGroup за бажання розширення проекту).
